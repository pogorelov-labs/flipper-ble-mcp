# Vendored from flipperzero_protobuf (BSD-3-Clause) — https://github.com/evilpete/flipperzero_protobuf_py
# Only the compiled protobuf message classes (protobuf-only) are vendored, to avoid the upstream
# numpy==1.22.3 pin that blocks fresh installs. License: see LICENSE in this directory.

